const db = require('./src/config/db'); async function t() { try { const [r] = await db.query('INSERT INTO clothes_entries (student_id, number_of_clothes, status) VALUES (?, ?, ?)', [4, 5, 'submitted']); console.log(r); await db.query('INSERT INTO transactions (clothes_entry_id, action_type) VALUES (?, ?)', [r.insertId, 'submitted']); console.log('success'); } catch(e) { console.log('Err', e.message); } process.exit(0); } t();